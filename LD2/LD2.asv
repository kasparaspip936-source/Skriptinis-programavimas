%Kasparas Pipiras
%EEf-25/2
%2026-09-16

clear
% 1.
a = 200:-10:10;
b = log10(a);
c = 10.^b;
d = c - a;

%% 2.
clear
A = [(pi/2) (3*1i) exp(pi); log2(2) (2*pi) log10(1); log(exp(1)) pi^pi cos(pi)];
A(1:3,2) = rand;
sum1 = sum(A(1,1:3));
sum2 = sum(A(2,1:3));
sum3 = sum(A(3,1:3));

%% 3.
clear
t = 0:0.002:2;
A = 7;
f = 8;
o = 2;
U1 = 4;
U2 = 3;
s = A*sin(2*pi*f*t) + 0.5*A*cos(4*pi*f*t);
n = o*randn(size(t));

S = s + n;

s1 = S(S>U1);

S(S<U2) = 0;

signalas = median(S);

size(s1)

didziausias_signalas = max(S);
maziausias_signalas = min(S);

%% P1.
clear
A = [0 1 0 2 3 0 4; 0 0 0 0 0 0 0; 0 5 0 6 7 0 8; 0 9 0 1 2 0 3; 0 0 0 0 0 0 0; 0 4 0 5 6 0 7];
B1 = A(:, 2:7);
B2 = B1(:, 1)
disp(A1)